apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    access: proxy
    url: db:5432
    database: hr_predictions
    user: postgres
    secureJsonData:
      password: postgres
    isDefault: true
    jsonData:
      sslmode: disable
